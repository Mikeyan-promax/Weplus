# 生产部署环境变量示例（复制为 .env 并按需修改）

# 日志与可观测性
LOG_JSON_ENABLED=true
PROMETHEUS_ENABLED=true
SENTRY_DSN=

# CORS 与管理安全
CORS_ALLOW_ORIGINS=https://example.com,https://admin.example.com
ADMIN_IP_WHITELIST=127.0.0.1,10.0.0.0/8

# 后端运行
UVICORN_WORKERS=2

# 数据库（如启用）
# DATABASE_URL=postgresql+psycopg2://user:pass@db:5432/weplus

